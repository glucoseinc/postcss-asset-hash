{
  "name": "postcss-asset-hash",
  "version": "0.0.1",
  "description": "postcss-asset-hash plugin",
  "main": "./lib/index.js",
  "scripts": {
     "build": "$(npm bin)/babel source --presets babel-preset-es2015 --out-dir distribution",
     "prepublish": "npm run build",
     "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
